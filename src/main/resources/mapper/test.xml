<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pro.demang.mapper.TestMapper">

	<select id="fList" resultType="org.pro.demang.model.MemberDTO">
		select m.m_email, m.m_nickname from followTBL f join memberTBL m where f.followee = m.m_id and f.follower = #{follower};
	</select>

	<select id="">
		
	</select>

</mapper>